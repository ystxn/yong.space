{
  "name": "yong.space",
  "version": "1.0.0",
  "license": "MIT",
  "repository": "https://github.com/ystan-/yong.space",
  "scripts": {
    "start": "preact watch --template src/template.html",
    "build": "preact build --template src/template.html && zip -r build.zip build && rm -rf build"
  },
  "devDependencies": {
    "preact-cli": "^3.0.0-rc.6",
    "preact-render-to-string": "^5.1.8"
  },
  "dependencies": {
    "preact": "^10.4.1",
    "react-icons": "^3.10.0",
    "typeface-jetbrains-mono": "^1.0.5"
  }
}
